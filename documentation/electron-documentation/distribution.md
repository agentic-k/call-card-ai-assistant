🧪 Dry‑run (simulate build + upload)
```npx electron-forge publish --platform=darwin --dry-run```
✅ Generates artifacts and metadata but no upload 



📬 Execute upload from dry‑run
```npx electron-forge publish --platform=darwin --from-dry-run```
✅ Uploads previously laid‑out artifacts—no rebuild 