
const { ipcRenderer } = require('electron');

// Simple preload script that ensures we can communicate with the main process
console.log('Auth preload script loaded');

// Add any future auth window specific functionality here if needed
